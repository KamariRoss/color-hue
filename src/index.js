import React from 'react'
import ReactDOM from 'react-dom'
// This i where were getting the render function
import './index.css'
// assumes the app is javascript
import App from './App'

// This is the file that starts the app
ReactDOM.render(<App />, document.getElementById('root'))
